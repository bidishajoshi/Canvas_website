import type { Metadata } from 'next';
import CustomizeTShirtPage, { generateMetadata as baseMetadata } from '../customize-tshirt/page';

export const generateMetadata = baseMetadata;
export default CustomizeTShirtPage;
